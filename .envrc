# direnv configuration for DOORway
# Run `direnv allow` to enable

# Load Nix flake environment
use flake

# Suppress verbose direnv output in terminal
export DIRENV_LOG_FORMAT=""

# Project-specific environment
export DOORWAY_DEV=1
